version: '3.8'

services:     
  postgres:
    image: postgres:latest
    container_name: postgres-development
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - /home/data/development/pgdata_dev:/var/lib/postgresql/data
    ports:
      - "5555:5432"